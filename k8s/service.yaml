apiVersion: v1
kind: Service
metadata:
  name: ms-productos-service
spec:
  selector:
    app: ms-productos
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8081
    nodePort: 30081
  type: NodePort
